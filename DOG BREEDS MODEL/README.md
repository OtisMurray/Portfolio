This project uses R to analyze data by normalizing features with `caret`, then reducing dimensionality with PCA, and finally clustering samples using a Gaussian Mixture Model `ClusterR`. The project separates feature work from model creation, transforms the data, and outputs probabilistic cluster assignments for later analysis about which dog breed was more likely.

The results were then judged based on how accurately the created models would perform against some unknown set of testing data.
