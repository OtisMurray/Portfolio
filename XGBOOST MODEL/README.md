This project predicts IC50 values for the Omicron variant using patient and covariate data. It utilizes R to perform data preprocessing, feature engineering, dimensionality reduction, and regression modeling with XGBoost.

It then saves the parameter tuning attempts in a log for future reference and again was judged based on its predictive ability for assessing an unknown testing dataset.
